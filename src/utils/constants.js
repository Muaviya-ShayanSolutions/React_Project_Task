/* eslint-disable no-unused-vars */
// APIs
export const BaseURL = "https://restcountries.com/v3.1/";
export const getAllCountries = `${BaseURL}all`;
export const getCountryDetail = `${BaseURL}name/`;
